import React from 'react';
import Layout from './Layout';
import {BrowserRouter, Link} from 'react-router-dom'

let App = props => {
  return (
    <div>
      <Layout />
    </div>
  );
}

export default App;
